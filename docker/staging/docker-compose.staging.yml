version: "3.9"

services:
  frontend:
    build:
      context: ../..
      dockerfile: Dockerfile
    container_name: qip_front_staging
    ports:
      - "8011:80"
    networks:
      - qip_backend_net
      - woodpecker_default
    restart: unless-stopped

networks:
  qip_backend_net:
    external: true
  woodpecker_default:
    external: true
